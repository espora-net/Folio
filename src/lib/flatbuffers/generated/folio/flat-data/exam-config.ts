// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class ExamConfig {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ExamConfig {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsExamConfig(bb:flatbuffers.ByteBuffer, obj?:ExamConfig):ExamConfig {
  return (obj || new ExamConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsExamConfig(bb:flatbuffers.ByteBuffer, obj?:ExamConfig):ExamConfig {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ExamConfig()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

numQuestions():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

numReserve():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

durationMinutes():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

pointsCorrect():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

pointsIncorrect():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

pointsBlank():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

passingScore():number {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.readFloat64(this.bb_pos + offset) : 0.0;
}

hasPassingScore():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

description():string|null
description(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
description(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

static startExamConfig(builder:flatbuffers.Builder) {
  builder.startObject(9);
}

static addNumQuestions(builder:flatbuffers.Builder, numQuestions:number) {
  builder.addFieldInt32(0, numQuestions, 0);
}

static addNumReserve(builder:flatbuffers.Builder, numReserve:number) {
  builder.addFieldInt32(1, numReserve, 0);
}

static addDurationMinutes(builder:flatbuffers.Builder, durationMinutes:number) {
  builder.addFieldInt32(2, durationMinutes, 0);
}

static addPointsCorrect(builder:flatbuffers.Builder, pointsCorrect:number) {
  builder.addFieldFloat64(3, pointsCorrect, 0.0);
}

static addPointsIncorrect(builder:flatbuffers.Builder, pointsIncorrect:number) {
  builder.addFieldFloat64(4, pointsIncorrect, 0.0);
}

static addPointsBlank(builder:flatbuffers.Builder, pointsBlank:number) {
  builder.addFieldFloat64(5, pointsBlank, 0.0);
}

static addPassingScore(builder:flatbuffers.Builder, passingScore:number) {
  builder.addFieldFloat64(6, passingScore, 0.0);
}

static addHasPassingScore(builder:flatbuffers.Builder, hasPassingScore:boolean) {
  builder.addFieldInt8(7, +hasPassingScore, +false);
}

static addDescription(builder:flatbuffers.Builder, descriptionOffset:flatbuffers.Offset) {
  builder.addFieldOffset(8, descriptionOffset, 0);
}

static endExamConfig(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createExamConfig(builder:flatbuffers.Builder, numQuestions:number, numReserve:number, durationMinutes:number, pointsCorrect:number, pointsIncorrect:number, pointsBlank:number, passingScore:number, hasPassingScore:boolean, descriptionOffset:flatbuffers.Offset):flatbuffers.Offset {
  ExamConfig.startExamConfig(builder);
  ExamConfig.addNumQuestions(builder, numQuestions);
  ExamConfig.addNumReserve(builder, numReserve);
  ExamConfig.addDurationMinutes(builder, durationMinutes);
  ExamConfig.addPointsCorrect(builder, pointsCorrect);
  ExamConfig.addPointsIncorrect(builder, pointsIncorrect);
  ExamConfig.addPointsBlank(builder, pointsBlank);
  ExamConfig.addPassingScore(builder, passingScore);
  ExamConfig.addHasPassingScore(builder, hasPassingScore);
  ExamConfig.addDescription(builder, descriptionOffset);
  return ExamConfig.endExamConfig(builder);
}
}
