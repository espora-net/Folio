// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { OriginCount } from '../../folio/flat-data/origin-count';
import { TopicCount } from '../../folio/flat-data/topic-count';


export class OptimizedCounts {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):OptimizedCounts {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsOptimizedCounts(bb:flatbuffers.ByteBuffer, obj?:OptimizedCounts):OptimizedCounts {
  return (obj || new OptimizedCounts()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsOptimizedCounts(bb:flatbuffers.ByteBuffer, obj?:OptimizedCounts):OptimizedCounts {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new OptimizedCounts()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

totalTopics():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

totalFlashcards():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

totalQuestions():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

originCounts(index: number, obj?:OriginCount):OriginCount|null {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? (obj || new OriginCount()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

originCountsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

topicCounts(index: number, obj?:TopicCount):TopicCount|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? (obj || new TopicCount()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

topicCountsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startOptimizedCounts(builder:flatbuffers.Builder) {
  builder.startObject(5);
}

static addTotalTopics(builder:flatbuffers.Builder, totalTopics:number) {
  builder.addFieldInt32(0, totalTopics, 0);
}

static addTotalFlashcards(builder:flatbuffers.Builder, totalFlashcards:number) {
  builder.addFieldInt32(1, totalFlashcards, 0);
}

static addTotalQuestions(builder:flatbuffers.Builder, totalQuestions:number) {
  builder.addFieldInt32(2, totalQuestions, 0);
}

static addOriginCounts(builder:flatbuffers.Builder, originCountsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(3, originCountsOffset, 0);
}

static createOriginCountsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startOriginCountsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addTopicCounts(builder:flatbuffers.Builder, topicCountsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, topicCountsOffset, 0);
}

static createTopicCountsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTopicCountsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endOptimizedCounts(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createOptimizedCounts(builder:flatbuffers.Builder, totalTopics:number, totalFlashcards:number, totalQuestions:number, originCountsOffset:flatbuffers.Offset, topicCountsOffset:flatbuffers.Offset):flatbuffers.Offset {
  OptimizedCounts.startOptimizedCounts(builder);
  OptimizedCounts.addTotalTopics(builder, totalTopics);
  OptimizedCounts.addTotalFlashcards(builder, totalFlashcards);
  OptimizedCounts.addTotalQuestions(builder, totalQuestions);
  OptimizedCounts.addOriginCounts(builder, originCountsOffset);
  OptimizedCounts.addTopicCounts(builder, topicCountsOffset);
  return OptimizedCounts.endOptimizedCounts(builder);
}
}
